{
    "name": "care-record-pro-firebase",
    "version": "1.0.0",
    "description": "Care Record Pro with Firebase Storage and Encryption",
    "dependencies": {},
    "scripts": {
        "build": "mkdir -p dist && cp index.html logo.png 404.html _redirects dist/"
    }
}